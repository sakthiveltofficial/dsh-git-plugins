# @dsh-git/local

Local git provider: implements `gitLocal` by shelling to the real git binary through `ctx.shell` (submodules, LFS, and hooks handled by git itself).

Part of the [dsh-git](https://github.com/sakthiveltofficial/dsh-git-plugins) plugin suite for DeepSeek Harness — see the root README for install and usage.
